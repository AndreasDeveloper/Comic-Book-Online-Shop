<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://unpkg.com/ionicons@4.4.4/dist/css/ionicons.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Raleway:300,400,500,700" rel="stylesheet">
    <title><%= currentUser.username %> Profile Settings - Comicbook</title>
</head>
<body>

    <!-- IMPLEMENTING NAVIGATION -->
    <% include ../partials/navigation.ejs %>

    <main>
    <div class="standard-wrapper">

        <section class="user-settings">
            <div class="settings-content">
                <h2 class="settings-content__heading">Edit Profile</h2>
                <form action="/user-profile/<%=userData.username.toLowerCase()%>?_method=PUT" method="POST" class="edit-form">
                    <input type="text" name="userData[username]" value="<%= userData.username %>" required>
                    <input type="email" name="userData[email]" value="<%= userData.email %>" required>
                    <input type="text" name="userData[bioShort]" value="<%= userData.bioShort %>" required>
                    <div class="edit-form__password">
                        <input type="password" name="userData[password]" value="<%= userData.password %>" id="settings-pw" required>
                        <i class="icon ion-ios-eye-off" id="icon-eye-on"></i>
                    </div>
                    <button type="submit" class="btn btn-login">Save Changes</button>
                </form>
            </div>
        </section>

        <!-- IMPLEMENTING FOOTER -->
        <% include ../partials/footer.ejs %>
    </div>
</main>

</body>
</html>